using UnityEngine;
using System.Collections;
using System.Xml;
using System.Collections.Generic;

public class CheckHighScore : MonoBehaviour {
/*
	void Start () {
		
		string url = "127.0.0.1/cf/gs.xml";
		
		WWWForm form = new WWWForm();
		form.AddField("var1", "value1");
		form.AddField("var2", "value2");
		WWW www = new WWW(url, form);
		

		StartCoroutine(WaitForRequest(www));
	}
	
	IEnumerator WaitForRequest(WWW www)
	{
		yield return www;
			
			// check for errors
			if (www.error == null)
		{
			Debug.Log("WWW Ok!: " + www.data);

			string xml = www.data;
			XmlDocument xmlDoc = new XmlDocument();
			xmlDoc.LoadXml(xml);   



	
			foreach(XmlElement node in xmlDoc.SelectNodes("thing"))
			{
				//tempPlayer.Id = int.Parse(node.GetAttribute("id"));
				Debug.Log ("HELLO HELLO HELLO >>>>>> " + node.SelectSingleNode("two").InnerText);
				Debug.Log ("HELLO HELLO HELLO2 >>>>>> " + node.InnerText);
				
				
			}

			www.Dispose();

		} else {
			Debug.Log("WWW Error: "+ www.error);
		}

		

	} */


	




}
